{
  "name": "eve-mocks-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun sync.ts && blume dev",
    "build": "blume build",
    "sync": "bun sync.ts",
    "validate": "bun sync.ts && blume build && blume validate"
  },
  "devDependencies": {
    "blume": "^1.7.2"
  }
}
